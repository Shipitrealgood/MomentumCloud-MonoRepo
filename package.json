{
  "name": "momentumcloud-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "clean": "turbo run clean",
    "clean:full": "rimraf .turbo && npm run clean && rimraf packages/mcp-servers/auth-mcp-server/node_modules packages/mcp-servers/ease-mcp-server/node_modules packages/mcp-servers/salesforce-mcp-server/node_modules packages/momentum-cloud/crm/node_modules packages/orchestrator-host/node_modules",
    "start": "turbo run start",
    "start:host": "turbo run start --filter=@mcp-agent/orchestrator-host",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "tsx": "^4.16.5",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}