// Export the main application controller as the primary entry point.
export { EaseApp } from './ease-app.js';

// You could also choose to export other key components if needed,
// but for now, just exporting EaseApp is the cleanest approach.